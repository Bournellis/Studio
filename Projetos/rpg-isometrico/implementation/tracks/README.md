# Active Tracks

This directory contains the live operational line for Godot-first work.

## Rules

- each track owns its own `current-status.md`, `implementation-map.md`, and `progress-log.md`
- tracks build on the accepted Godot baseline in `implementation/current-status.md`
- historical validation material under `phase-g1/` through `phase-g4/` is context, not the default workflow

## Active Track

- `track-02-canonical-product-foundation/`

## Preserved Baseline Track

- `track-01-playable-base-and-real-backlog/` remains the B0 backlog and accepted local baseline reference
