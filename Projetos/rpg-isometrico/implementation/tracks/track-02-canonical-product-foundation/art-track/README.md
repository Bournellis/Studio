# Art Track

This track runs in parallel with engineering, but it is phase-coupled.

Rules:

- art works one half-step ahead of implementation whenever possible
- proxy assets are valid phase inputs
- final production art cannot block functional acceptance of a phase
- every phase gate needs an explicit art manifest

Read next:

1. `foundation-style-and-proxy-kit.md`
2. the active phase gate under `../gates/`
