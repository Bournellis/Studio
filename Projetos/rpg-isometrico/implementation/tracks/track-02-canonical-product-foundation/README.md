# Track 02 - Canonical Product Foundation

This track owns the shift from the accepted local multi-mode base into the canonical product loop.

It treats the old playable base as `B0` internal foundation and opens the next work through explicit phase gates.

Read order:

1. `current-status.md`
2. `implementation-map.md`
3. the explicit next gate only after it is selected in `current-status.md`
4. `art-track/README.md`
5. `progress-log.md`

Historical note:

- Earlier gates, including `gates/gate-f01-meta-foundation-and-tutorial.md`, preserve implementation context only and are not active read-order items unless a task explicitly needs their history.
