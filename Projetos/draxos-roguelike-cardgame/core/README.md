# Core

Foundation contracts, identifiers, snapshots, result objects, and domain-neutral helpers.

This layer should stay independent from visual presentation and from Godot node hierarchy choices wherever practical.

Current session state owns the selected 20-card deck, active encounter id, pre-combat snapshot, and battle result summary.
