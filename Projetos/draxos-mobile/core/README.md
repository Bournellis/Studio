# core/

Contratos GDScript, tipos compartilhados e helpers sem dependencia de modos especificos.

Arquivos atuais:

- `project_info.gd` - constantes de bootstrap e escopo MVP.
- `ui_tokens.gd` - autoload `UiTokens`, cores e estilos semanticos.
- `asset_ids.gd` - autoload `AssetIds`, manifesto de ids visuais e fallback de arte ausente.

Helpers de comunicacao HTTP com Supabase entram em T00-P06.
