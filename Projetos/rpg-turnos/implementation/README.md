# RPG Turnos Implementation

This directory tracks active implementation status and future work tracks for `rpg-turnos`.

Start with `current-status.md`. It is a short operational snapshot, not a detailed implementation archive.

Detailed implementation records belong in the active track under `tracks/`, not in shared canon and not in the top-level status snapshot.

Current long-form runtime record:

- `tracks/track-01-foundation-first-prototype/foundation-runtime-record.md`
