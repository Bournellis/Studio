# RPG Turnos Implementation

This directory routes the paused technical state and compact implementation history for `rpg-turnos`.

Start with `current-status.md`. It is a short operational snapshot, not a detailed implementation archive.

Detailed closed results live in `history.md` and `history-ledger/`, not in shared canon and not in the top-level status snapshot.

Exact pre-cutover records are recovery-only through the Documentation Lite receipt and baseline Git. No active track exists while the project remains paused.
