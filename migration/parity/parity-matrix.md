# Historical Parity Snapshot

This file records the comparative read captured around the initial Godot validation cycle.

It is archival context only and must not be treated as an active planning surface.

## Legend

- `Shared Canon`: defined and authoritative
- `Unity Legacy`: what existed in the older runtime at cutover time
- `Godot Validation`: what the G1-G4 cycle proved locally

| Area | Shared Canon | Unity Legacy | Godot Validation |
| --- | --- | --- | --- |
| Product identity | Yes | Consult only | Implemented in the accepted Godot baseline |
| Loadout contract | Yes | Implemented | Implemented in the accepted local baseline |
| Frontend to playable loop | Yes | Implemented | Implemented in authored scenes with stable UI anchors |
| Arena combat core | Yes | Implemented | Implemented with telemetry, bot telegraph, explicit round flow, camera impact feedback, bot reposition pacing, clarity layers, and staged feedback for the four validation skills |
| Simple bot opponent | Allowed | Implemented via Arena Bot surface | Implemented in the validation slice |
| Result and return flow | Yes | Implemented | Implemented with summary stats, linger, and clean return |
| Solo local runtime | Yes | Implemented | Implemented and accepted |
| PC-first controls | Yes | Implemented | Implemented and accepted |
| Mobile controls | Future/engine-local | Implemented | Deferred |
| Online runtime | Future slice | Implemented in legacy seams | Deferred |
| Steam services | Future slice | Planned in legacy docs | Deferred |

## Historical Read

- G1-G4 reached and approved the Godot validation target.
- The active workflow no longer uses this matrix as a day-to-day decision surface.
