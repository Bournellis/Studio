# Migration Archive

This directory preserves the historical cutover from the older Unity line into the Godot-first workspace.

## Purpose

Use these files only when you need historical context about:

- what moved into shared canon during the cutover
- how the original Unity-to-Godot mapping was framed
- what the validation team recorded at the time of transition
- why the workspace now treats legacy material as archival rather than operational

## Non-Role

This directory is not part of the daily read order for active work.

Active implementation flow lives under `Projetos/rpg-isometrico/implementation/`.
